#!/usr/bin/env bash
set -euo pipefail

mkdir -p "$HOME/Software/RPI_Imager"
wget -O "$HOME/Software/RPI_Imager/rpi_imager.deb" 'https://downloads.raspberrypi.org/imager/imager_latest_amd64.deb'
sudo apt-get install -y "$HOME/Software/RPI_Imager/rpi_imager.deb"
