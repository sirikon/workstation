environment:
  CONFIG_HOME: $HOME/.config/sway

tasks:
  default:
    labels:
      autorequire: "true"
      apt.packages: "sway"
    quiet: true
    script: |
      rm -rf "$CONFIG_HOME"
      ln -fFs "$(pwd)/config" "$CONFIG_HOME"
